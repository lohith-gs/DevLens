# DevLens
Chrome DevTools extension for serious API debugging. Inspect JSON responses in an interactive tree, diff two requests side by side, generate TypeScript interfaces from any response, visualize request timelines, and get first-class GraphQL support — all without leaving DevTools.
